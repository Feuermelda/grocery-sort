<Window x:Class="Grocery_Sort.CreateList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Grocery_Sort"
        mc:Ignorable="d"
        Title="CreateList" Height="600" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" 
        Background="#fffced">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Title Header -->
            <RowDefinition Height="*" />
            <!-- List Create Area -->
            <RowDefinition Height="Auto" />
            <!-- Save & Cancel Buttons -->
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="PaleGoldenrod" BorderThickness="2" CornerRadius="8" Margin="5"  Background="#fffefa">
            <TextBlock x:Name="Create" Text="Create List" FontSize="20" Foreground="#222" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="10" />
        </Border>

        <Border Grid.Row="1" BorderBrush="PaleGoldenrod" BorderThickness="2" CornerRadius="8" Margin="5" Background="#fffefa" >
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- List Name and Layout -->
                    <RowDefinition Height="*"/>
                    <!-- List Content -->
                    <RowDefinition Height="Auto"/>
                    <!-- Add Item Button -->
                </Grid.RowDefinitions>


                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition MaxHeight="200"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Listname" FontSize="13" Margin="5" Padding="5" VerticalAlignment="Center" Foreground="#222"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="EnterListName" FontSize="13" BorderThickness="1" Margin="5" Padding="5" VerticalAlignment="Center" Background="#fffffa" Foreground="#222"/>
                    <TextBlock Grid.Row="1" Text="Layout" FontSize="13" Margin="5" Padding="5" VerticalAlignment="Center" Foreground="#222"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding LayoutList}"  DisplayMemberPath="Name" SelectionChanged="Layout_SelectionChanged" x:Name="SelectLayout" 
                                  Foreground="#222" BorderThickness="0" Margin="5" Padding="5" VerticalAlignment="Center" HorizontalContentAlignment="Center" >
                    </ComboBox>
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Favorite Items" FontSize="13" Margin="5" Padding="5" VerticalAlignment="Center" Foreground="#222"/>
                    <Grid Grid.Row="2" Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition MaxHeight="120"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ListBox ItemsSource="{Binding CurrentFavorites}" Grid.Row="0" Foreground="#222" BorderThickness="0" 
                             Margin="5" Padding="5" VerticalAlignment="Center" HorizontalContentAlignment="Center">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                        <TextBlock>
                                            <Run Text="("/>
                                            <Run Text="{Binding Amount}"/>
                                            <Run Text=") "/>
                                            <Run Text="{Binding Name}"/>
                                            <Run Text=" ["/>
                                            <Run Text="{Binding ItemCategory}"/>
                                            <Run Text="]"/>
                                        </TextBlock>

                                    </CheckBox>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <Button Grid.Row="1" Width="200" Content="Add" Click="AddFavorites_Click"/>

                    </Grid>

                </Grid>


                <ListBox Grid.Row="1" ItemsSource="{Binding NewList.Items}" BorderThickness="0,2,0,0" BorderBrush="PaleGoldenrod" Margin="5" HorizontalContentAlignment="Stretch" Foreground="#222">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" VerticalAlignment="Center" Foreground="#222">
                                            <Run Text="{Binding Name}"/>
                                </TextBlock>
                                <TextBlock Grid.Column="1">
                                <Run Text=" ["/>
                                            <Run Text="{Binding ItemCategory}"/>
                                            <Run Text="]"/>
                                </TextBlock>
                                <TextBlock Grid.Column="2">
                                            <Run Text=" ("/>
                                            <Run Text="{Binding Amount}"/>
                                            <Run Text=")"/>
                                </TextBlock>

                                <Button Grid.Column="3" Content="X" BorderThickness ="0" Click="RemoveItem_Click"
                                        FontWeight="Bold" Foreground="Red" Background="Transparent"
                                        HorizontalAlignment="Right" Margin="0,0,5,0"/>

                                <Button Grid.Column="4" Content="✏️" BorderThickness ="0" Click="EditItem_Click"
                                        FontWeight="Bold" Background="Transparent"
                                        HorizontalAlignment="Right" Margin="5,0,0,0"/>

                                <Button Grid.Column="5" BorderThickness ="0" Click="Favorite_Click"
                                        FontWeight="Bold" Background="Transparent"
                                        HorizontalAlignment="Right" Margin="5,0,0,0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Content" Value="🤍"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsFavorite}" Value="true">
                                                    <Setter Property="Content" Value="❤️"/>
                                                    <Setter Property="Foreground" Value="PaleVioletRed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Button Grid.Row="2" Content="Add Item" Click="AddItem_Click" FontSize="13" Margin="10" Height="50" Background="#e0dac1" Foreground="#222"/>

            </Grid>


        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Cancel" Click="Cancel_Click" FontSize="13" Padding="0,10" Margin="5" Background="#e0dac1" Foreground="#222"/>
            <Button Grid.Column="1" Content="Save" Click="SaveList_Click" FontSize="13" Padding="0,10" Margin="5" Background="#e0dac1" Foreground="#222"/>
        </Grid>

    </Grid>
</Window>
